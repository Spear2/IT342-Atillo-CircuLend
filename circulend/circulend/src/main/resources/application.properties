spring.application.name=circulend
#Database setup
spring.datasource.url=jdbc:mysql://localhost:3306/dbcirculendg2atillo
spring.datasource.username=root
spring.datasource.password=Ron2263$

#JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


# JWT signing key. In production set JWT_SECRET env var; do not commit a real secret.
# Must be at least 32 characters for HS256.
app.jwt.secret=${JWT_SECRET:change-me-use-env-var-in-production-min-32-chars}